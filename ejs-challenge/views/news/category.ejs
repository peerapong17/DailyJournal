<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= category.substring(0,1).toUpperCase() + category.substring(1) %></title>
  </head>
  <body>
    <div class="container">
      <%- include('../partials/header') -%>
      <h1 class="my-4 text-center"><%= category.substring(0,1).toUpperCase() + category.substring(1) %></h1>
      <% posts.map((post)=>{ %>
      <div class="mt-5 text-center">
        <img src="<%= post.image %>" alt="<%= post.title %>" width="700px" height="350px">
        <h6 class="mt-3">
          <%= post.title %><span class="badge badge-pill badge-success ml-2"
            ><%= post.category %></span
          >
        </h6>
        <p>
          <%- post.content.substring(1, 120) %>
          <a href="/news/<%= post._id %>">ReadMore...</a>
        </p>
      </div>
      
      <% }) %>
    </div>
    <!-- <div class="footer-padding">
      <div class="footer">
        <p>Made with ❤️ by The App Brewery</p>
      </div>
    </div> -->
  </body>
</html>
